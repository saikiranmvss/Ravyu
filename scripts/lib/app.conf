# App-specific deployment configuration — Ravyu
APP_SLUG="${APP_SLUG:-ravyu}"
APP_PORT="${APP_PORT:-5200}"
NODE_VERSION="${NODE_VERSION:-24}"
PACKAGE_MANAGER="${PACKAGE_MANAGER:-pnpm}"
FULL_PNPM_INSTALL="${FULL_PNPM_INSTALL:-true}"
SYSTEMD_UNIT="${SYSTEMD_UNIT:-ravyu-api}"
HEALTH_PATH="${HEALTH_PATH:-/api/healthz}"
DB_ENGINE="${DB_ENGINE:-mysql}"
DB_NAME_DEFAULT="${DB_NAME_DEFAULT:-ravyu}"

API_WORKDIR_REL="."
API_START_CMD="node --enable-source-maps artifacts/api-server/dist/index.mjs"
WEB_ROOT_REL="artifacts/ravyu/dist/public"
UPLOAD_SYMLINK_REL=""
MIGRATION_CMD="node scripts/apply-migrations.mjs"
CLIENT_MAX_BODY_SIZE="${CLIENT_MAX_BODY_SIZE:-20m}"
ENABLE_WEBSOCKET="${ENABLE_WEBSOCKET:-false}"
